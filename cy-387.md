请根据以下要求生成项目代码和 README。

## 项目主要需求

### 项目概述
**项目类型：全栈Web应用**
搭建一个租房信息平台，房东可发布房源，租客可搜索筛选房源，同时集成物业报修功能，方便住户提交和跟踪维修进度。

### 功能需求

1. **房源发布**：房东填写小区名称、户型（几室几厅）、面积、租金、押金、付款方式（月付/季付/年付）、房屋照片、配套设施（空调/洗衣机/宽带等），发布房源信息。
2. **房源搜索与筛选**：租客按区域、价格范围、户型、面积、配套设施等条件筛选房源，结果列表和地图模式切换查看。
3. **房源详情**：查看房屋照片轮播、详细描述、配套设施清单、房东联系方式，支持收藏房源。
4. **预约看房**：租客选择看房时间段预约，房东确认后双方收到通知。
5. **租赁合同管理**：线上生成租赁合同模板，记录租期、租金、双方信息，双方可查看合同状态。
6. **物业报修**：住户提交报修工单（故障类型：水电/门锁/管道/家电/其他 + 故障描述 + 照片），物业人员接单处理，住户可跟踪维修进度。
7. **用户角色区分**：房东（发布房源、管理预约、查看合同）、租客（搜索房源、预约看房、提交报修）、物业人员（处理报修工单）。

### 技术要求

- 前端：Vue 3 + TypeScript + Element Plus，构建工具 Vite
- 后端：Python + Django + Django REST Framework
- 数据库：PostgreSQL
- 认证：JWT
- 地图：高德地图 JS API（房源地图展示）
- 图片存储：后端本地 media 目录
- 代码规范：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、房屋状态枚举、报修类型）统一维护在后端 `backend/app/constants/` 目录下；日志统一使用 Python logging 模块，日志配置在 `backend/app/utils/logger.py`；异常信息统一定义在 `backend/app/constants/errors.py`，后端通过 DRF 自定义异常处理统一返回标准格式响应。

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面，组件，路由，状态管理，API 请求，类型定义，样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

后端目录结构建议：
```
backend/
├── app/
│   ├── apps/               # Django App（按模块拆分）
│   │   ├── users/          # 用户模块
│   │   ├── properties/     # 房源模块
│   │   ├── booking/        # 预约看房模块
│   │   ├── contract/       # 合同模块
│   │   └── repair/         # 报修模块
│   ├── constants/          # 常量、错误码、枚举
│   ├── utils/              # 工具函数（logger, jwt 等）
│   ├── middleware/          # 中间件
│   └── settings.py         # Django 配置
├── manage.py
├── database/
│   └── init.sql
└── requirements.txt
```

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=rentfind`
- `frontend/Dockerfile`：多阶段构建
- `backend/Dockerfile`：Django 后端运行镜像
- `frontend/nginx.conf`
- `.env.example`

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: rentfind`
- 容器名带 `rentfind-` 前缀
- 数据库数据使用命名卷持久化
- 前端 Nginx 反向代理 `/api` → `backend:8000`
- 数据库配置 `healthcheck`
- 端口映射：
  - 前端：18407:80
  - 后端：19407:8000

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目名称和一句话介绍、项目主要功能、快速启动方式（Docker Compose）、本地开发方式、访问地址（http://localhost:18407）、技术栈（表格展示）、项目目录结构（代码块展示）、环境变量说明、Docker 部署说明、License。

要求：Docker 部署命令放在最前面；告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   node_modules/
   dist/
   __pycache__/
   *.pyc
   .env
   .DS_Store
   *.log
   venv/
   media/
   ```
3. 执行 `git add -A && git commit -m "init: 项目初始化"`
